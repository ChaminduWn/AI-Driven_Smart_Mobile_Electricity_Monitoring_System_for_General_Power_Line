PYTEST_INI = """
[pytest]
# Test discovery patterns
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
addopts = 
    -v
    --tb=short
    --strict-markers
    -ra
    --color=yes
    --disable-warnings

# Markers
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
    unit: marks tests as unit tests
    api: marks tests as API tests
    service: marks tests as service layer tests

# Coverage (if using pytest-cov)
# Uncomment if you want coverage reports
# addopts = --cov=src --cov-report=html --cov-report=term

# Logging
log_cli = false
log_cli_level = INFO

# Timeout (if using pytest-timeout)
# timeout = 300

# Parallel execution (if using pytest-xdist)
# addopts = -n auto
"""


print("=" * 70)
print("TEST FILES CREATED")
print("=" * 70)
print("\nCreate these files:")
print("1. tests/test_extractor.py")
print("2. tests/test_api.py")
print("3. tests/conftest.py")
print("4. pytest.ini (in project root)")
print("\nTo run tests:")
print("  pytest tests/ -v")
print("\nTo run specific test file:")
print("  pytest tests/test_api.py -v")
print("\nTo run with coverage:")
print("  pytest tests/ --cov=src --cov-report=html")
print("\n" + "=" * 70)